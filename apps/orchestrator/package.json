{
  "name": "@planetloga/orchestrator",
  "version": "0.0.1",
  "private": true,
  "description": "Orchestrator-Agent für automatische Aufgabenverteilung",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@planetloga/types": "workspace:*",
    "@planetloga/sdk-ts": "workspace:*",
    "@planetloga/protocol": "workspace:*"
  }
}
