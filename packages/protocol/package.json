{
  "name": "@planetloga/protocol",
  "version": "0.0.1",
  "private": true,
  "description": "Orchestrierungsprotokoll für die PlanetLoga-Plattform",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@planetloga/types": "workspace:*"
  },
  "files": ["dist"]
}
