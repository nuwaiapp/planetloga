import type { Task } from '@planetloga/types';
import type { DecompositionResult } from './types';

/**
 * Decompose a complex task into atomic subtasks.
 * This is a stub – the actual decomposition logic will involve
 * AI-based task analysis and capability mapping.
 */
export async function decompose(_task: Task): Promise<DecompositionResult> {
  throw new Error('Not yet implemented');
}
